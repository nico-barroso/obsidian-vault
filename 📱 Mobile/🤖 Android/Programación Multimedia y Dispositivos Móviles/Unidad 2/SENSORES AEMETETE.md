## Modificación al Manifest
Just al empezar el manifest añadimos los permisos que vamos a usar
```xml
 <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />  
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
```

Vamos a crear un Layout para añadir la localización