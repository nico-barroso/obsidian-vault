
El eslint es el archivo de configuración del eslint, que corrige los fallos de código y genera reglas. Se pueden escoger que fichers, los lenguajes

rules: {
'no-unused-vars' - marca como error el archivo una variable vacia
}